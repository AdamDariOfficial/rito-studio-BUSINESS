# RITO Studio — START → BUSINESS Inheritance Contract

**Updated:** 6 September 2026
**Status:** `ACTIVE_BY_FROZEN_START_TAG`
**Applies to:** RITO Studio BUSINESS
**Canonical START:** `family-start-v1.0` → `74ee03c4d39a974872f94f53d14ec2873815ccf7`
**BUSINESS remix baseline:** `222c331db44b1775aa2f877634f3a0f3dfdfbe69`

## Activation evidence

The contract is active because:

1. the START implementation and documentation closure PRs were merged;
2. the canonical START merge commit was synchronized and verified;
3. RITO Studio START was explicitly approved by the user;
4. the user explicitly authorized the START freeze;
5. the earlier SHA-only freeze exception remains part of history, and the later final freeze is now
   identified by the remotely published annotated tag `family-start-v1.0`;
6. the user explicitly authorized RITO Studio BUSINESS;
7. START was remixed in Lovable into a separate BUSINESS project;
8. the BUSINESS project was connected to its own GitHub repository;
9. the local BUSINESS clone and remix delta were verified;
10. the user authorized the identity and documentation bootstrap before implementation.

The earlier SHA-only freeze was a user-approved historical process exception. For all new BUSINESS
parity work, that exception is superseded by the final remotely published annotated tag
`family-start-v1.0`, which dereferences exactly to the current frozen START commit.

## Canonical sources

The authoritative family source is:

```text
repository: AdamDariOfficial/rito-studio-START
tag: family-start-v1.0
commit: 74ee03c4d39a974872f94f53d14ec2873815ccf7
```

The authorized BUSINESS working baseline is:

```text
repository: AdamDariOfficial/rito-studio-BUSINESS
commit: b6a82f918370f730681e9e0c0572a7a653d2dfeb
```

The historical remix baseline `222c331db44b1775aa2f877634f3a0f3dfdfbe69` contains the full
then-current START history plus two Lovable-managed commits whose net diff is limited to
`package.json` and `bun.lock`. The current BUSINESS source baseline shown above also contains the
subsequent approved multipage implementation and parity work; the `2.8.5` Lovable config pin is
preserved.

Do not derive BUSINESS from an earlier START candidate, a chat reconstruction, an
unverified archive, the rejected V1 visual prototype or Hospitality code as a visual
default.

## Inherited identity

BUSINESS must preserve the approved START identity unless a later approved decision
explicitly changes it:

- RITO Studio concept and Beauty & Care Atelier positioning;
- tagline `La bellezza, nel suo ritmo.`;
- porcelain, ink and burgundy palette;
- canonical accent `#6A3F4B`;
- Newsreader and Manrope typography;
- premium editorial composition and tactile photography;
- restrained cards and purposeful motion;
- Tretnix footer attribution;
- client-like demo presentation with honest non-operational data.

The detailed source remains `docs/DESIGN.md`, `docs/CONTENT.md`,
`docs/ANIMATIONS.md` and the canonical START implementation.

## Inherited technical behavior

BUSINESS must retain or improve:

- mobile-first behavior and absence of unintended horizontal overflow;
- text-first order for editorial mobile sections, with documented exceptions;
- route-top reset without smooth scrolling;
- intentional same-page anchor behavior;
- Back, Forward, refresh and direct URL behavior;
- visible focus, semantic landmarks and correct heading structure;
- drawer Escape handling, focus containment/return and body-scroll restoration;
- `prefers-reduced-motion`;
- no fictional reviews, metrics, credentials or commercial claims;
- no weakened authentication, authorization or RLS in any later backend scope.

The detailed source remains `AGENTS.md`, `docs/TESTING.md`, `docs/ROUTES.md`,
`docs/DECISIONS.md` and the canonical START implementation.

## Active BUSINESS scope

The current approved BUSINESS product includes:

- `/trattamenti` con filtri e dettaglio query-driven in dialog/sheet accessibile;
- `/studio`;
- `/galleria` with an accessible lightbox;
- `/faq`;
- `/contatti`;
- route-specific metadata and deeper content;
- centralized external conversion actions with `booking = WhatsApp + phone` and
  `contact = email + phone`;
- comparative START ↔ BUSINESS QA.

`/team` and `/prenota` are not part of the base BUSINESS route inventory after
`BW-DEC-044`. Team remains only a possible future optional module.

After `BW-DEC-045`, `/trattamenti/:slug` is also outside the active base BUSINESS route
inventory. Treatment detail remains available inside the catalogue through the
validated `categoria` and `trattamento` query state.

These are intentional product differences. They must not become an unrelated visual
redesign.

## Changes requiring a new decision

The following require explicit scope and approval:

- changing palette, typography or core visual personality;
- replacing the approved navigation, footer or CTA language globally;
- removing accessibility behavior;
- introducing backend, authentication, database, payments or native booking;
- introducing CRM, admin or management functionality;
- copying Hospitality identity or components as visual defaults;
- beginning BUSINESS PLUS.

## Current authorized phase

The current phase is the final-freeze reconciliation of the existing BUSINESS repository from
`main@b6a82f918370f730681e9e0c0572a7a653d2dfeb` against `rito-studio-START@family-start-v1.0`. The controlled application branch is
`feat/rito-business-final-freeze-reconciliation`.

The pass is limited to the documentation lineage/current-state correction and compatible shared
interaction/parity work recorded in `BW-DEC-049` through `BW-DEC-059`. Lovable remains passive. The
versioned Lovable prompt and Project Knowledge are historical adapters and must be regenerated before
any future use. The final application/browser candidate has passed its pre-stage gates; BW-DEC-059
authorizes the single Git closure sequence. Deployment, domains and infrastructure remain separate.


## Parity review before BUSINESS freeze

A future BUSINESS review must verify at minimum:

- palette and typography parity;
- navbar, drawer, CTA and footer parity;
- spacing and editorial rhythm;
- image treatment;
- motion and reduced-motion parity;
- mobile ordering and overflow;
- route, refresh, Back and Forward behavior;
- honest booking/contact adapter behavior (`WhatsApp + phone` / `email + phone`);
- absence of BUSINESS PLUS scope.

With brand name and logo hidden, START and BUSINESS must still be recognizable as the
same family.

## Final START lineage reconciliation — 6 September 2026

The canonical START source for all new BUSINESS parity work is now:

```text
repository: AdamDariOfficial/rito-studio-START
tag: family-start-v1.0
commit: 74ee03c4d39a974872f94f53d14ec2873815ccf7
```

The annotated tag is remotely published and dereferences exactly to the frozen commit. The BUSINESS
repository is not recreated. It keeps its approved multipage architecture, package/lockfile remix
state and BUSINESS-specific decisions. Historical parent SHAs remain lineage evidence rather than
current parity sources.

The current BUSINESS source baseline is:

```text
repository: AdamDariOfficial/rito-studio-BUSINESS
main: b6a82f918370f730681e9e0c0572a7a653d2dfeb
```

The 6 September reconciliation imports only shared patterns compatible with the active BUSINESS
contract, including `TRX-DEC-038` and `TRX-DEC-039`, while preserving BW-DEC-048 conversion
semantics.

## Public parity and conversion contract - 4 September 2026

The approved BUSINESS conversion contract is:

```text
booking = WhatsApp + phone
contact = email + phone
```

This remains an external-contact adapter only. BUSINESS does not gain a native booking form,
`/prenota`, availability, database, authentication or appointment persistence.

Public treatment/gallery UX may match BUSINESS PLUS where the pattern is presentation-only and
compatible with BUSINESS. PLUS-only consultation, admin, auth, realtime and backend behavior must
not be imported.

## BUSINESS freeze identification protocol — 7 September 2026

After the approved reconciliation PR is merged, the resulting `main` SHA must be verified locally and
remotely. The annotated tag:

```text
family-business-v1.0
```

must point exactly to that verified SHA and use the approved message:

```text
RITO Studio BUSINESS final frozen baseline — approved 2026-09-07
```

The tag and Git/GitHub metadata are the authoritative freeze record. Do not create a subsequent commit
only to repeat the merge SHA in repository documentation. BUSINESS PLUS may begin only after the tag
has been pushed and independently dereferenced to the merged BUSINESS `main` commit.
